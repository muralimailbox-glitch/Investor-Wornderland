import { test } from './helpers/fixtures';

test.describe('Full Journeys', () => {
  test.fixme('manual investor -> invite -> verify -> nda -> lounge -> ask -> book meeting', async () => {
    // TODO
  });

  test.fixme('csv imported investor -> invite -> verify -> nda -> document view -> founder reply', async () => {
    // TODO
  });

  test.fixme('unknown self serve nda investor -> sign -> founder sees new lead in pipeline', async () => {
    // TODO
  });

  test.fixme('pre nda investor asks sensitive question -> gets gated -> signs nda -> asks again', async () => {
    // TODO
  });

  test.fixme('investor sees locked document pre meeting -> books meeting -> document unlocks', async () => {
    // TODO
  });

  test.fixme('investor replies to outreach email -> inbox sync imports -> founder drafts and sends reply', async () => {
    // TODO
  });

  test.fixme('revoked invite blocks further ask access while preserving prior audit trail', async () => {
    // TODO
  });

  test.fixme('document previously viewable becomes inaccessible after expiry', async () => {
    // TODO
  });

  test.fixme('bootstrap route is disabled by default in deployed mode', async () => {
    // TODO
  });

  test.fixme('entire investor lifecycle leaves coherent state across lead nda docs ask meeting inbox outbox and audit', async () => {
    // TODO
  });
});
